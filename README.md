# benchmark-script

1, copy iperf-client-config-tpl.yaml and iperf-server-config.yaml to example folder

2, copy passthrough and zeromq folder to _workspace

3, copy test.sh to _workspace

4, ./test.sh
